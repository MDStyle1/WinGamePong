plugins {
    id 'java'
}

group 'com.mds'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    //implementation files('lib/JavaGame.main.jar')
    compile files('lib/JavaGame.main.jar')
}

jar {
    manifest {
        attributes "Main-Class": "com.mds.wingame.WinGame"
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
